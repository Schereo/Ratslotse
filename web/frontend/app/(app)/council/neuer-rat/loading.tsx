// Sofort-Feedback beim Seitenwechsel (Design 29a, P3) — wie bei thema/.
import { DetailSkeleton } from "@/components/ui";

export default function Loading() {
  return (
    <div>
      <DetailSkeleton />
    </div>
  );
}
