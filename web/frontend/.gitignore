node_modules/
.next/
out/
next-env.d.ts
.env
.env.local
*.tsbuildinfo

# Playwright artifacts (generated, not source)
playwright-report/
test-results/

# Capacitor: commit the ios/ and android/ projects, but not their build outputs,
# dependencies, or the copied web assets (regenerated by `cap sync`).
app/_api.disabled/
ios/App/Pods/
ios/App/build/
# Gerätebuilds (xcodebuild -derivedDataPath build-device, s. MOBILE.md)
ios/App/build-device/
ios/App/App/public/
ios/App/App/capacitor.config.json
ios/App/App/config.xml
# Export-Optionen fürs Release-Archiv — lokal einmal anlegen (Vorlage in MOBILE.md).
# Bewusst außerhalb des Repos, damit niemand versehentlich mit fremder teamID
# oder mit manageAppVersionAndBuildNumber=true exportiert.
ios/App/ExportOptions.plist
android/.gradle/
android/build/
android/app/build/
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/res/xml/config.xml
android/capacitor-cordova-android-plugins/
**/xcuserdata/

# Technik-Doku: baut docs-site/, kopiert der Deploy nach public/docs (nie committen)
/public/docs/
