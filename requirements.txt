requests>=2.31
PySocks>=1.7          # socks5h-Proxy für requests (kern/proxy.py)
python-dotenv>=1.0
openai>=1.30
tenacity>=8.2
beautifulsoup4
pypdf>=4.0
# Native-app push delivery (nwz/push.py): APNs needs HTTP/2 (httpx[http2]) and
# ES256 signing (cryptography); FCM v1 uses a service account (google-auth).
# All optional — nwz.push degrades to a no-op when unconfigured/uninstalled.
httpx[http2]>=0.27
# Streaming-Transkription des Livestreams (council/stream_stt.py): Websocket
# zu Gladia. uvicorn[standard] bringt das Paket auch mit — hier steht es,
# weil der Cron-Job es braucht, nicht der Webdienst.
websockets>=13
cryptography>=42.0
google-auth>=2.29
